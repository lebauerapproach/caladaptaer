% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drivers.R
\name{cae_build_met_drivers}
\alias{cae_build_met_drivers}
\title{Build CF-standard met driver files for multiple sites}
\usage{
cae_build_met_drivers(
  sites,
  model = "CESM2",
  scenario = "ssp370",
  start_year,
  end_year,
  outdir,
  resolution = "d01",
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{sites}{data.frame with columns \code{site_id}, \code{lon}, \code{lat} (WGS84
decimal degrees). See Examples.}

\item{model}{character; WRF GCM name. Default \code{"CESM2"}.
See \code{cae_models(activity = "WRF")} for options.}

\item{scenario}{character; SSP experiment. Default \code{"ssp370"}.
See \code{cae_scenarios()} for options.}

\item{start_year}{integer; first year to process}

\item{end_year}{integer; last year to process}

\item{outdir}{character; root output directory. Files are written to
\verb{\{outdir\}/\{site_id\}/\{model\}.\{scenario\}.\{year\}.nc}.}

\item{resolution}{character; WRF grid label. Default \code{"d01"} (45 km).}

\item{overwrite}{logical; overwrite existing files? Default \code{FALSE}.}

\item{verbose}{logical; print progress messages? Default \code{TRUE}.}
}
\value{
A data.frame with columns \code{site_id}, \code{year}, \code{file} (invisibly).
}
\description{
Fetches WRF data from S3 once per variable per year and extracts all
sites in a single vectorized call via \code{\link[=cae_fetch_points]{cae_fetch_points()}}, then writes
one CF-compliant NetCDF per site per year. For N sites, 8 variables,
and Y years this makes 8*Y S3 requests regardless of N.
}
\details{
Output files follow the PEcAn CF Meteorology convention and can be
registered in BETY or used directly by ecosystem models that read
CF-standard met inputs.
}
\examples{
\dontrun{
sites <- data.frame(
  site_id = c("fresno", "la", "sacramento"),
  lon = c(-119.77, -118.24, -121.49),
  lat = c(36.75, 34.05, 38.58)
)
result <- cae_build_met_drivers(
  sites, model = "CESM2", scenario = "ssp370",
  start_year = 2050, end_year = 2051,
  outdir = "met_output"
)
head(result)
}
}
\seealso{
\code{\link[=cae_fetch_points]{cae_fetch_points()}} for the underlying multi-site extraction,
\code{\link[=cae_write_cf_netcdf]{cae_write_cf_netcdf()}} for the stars-based single-grid approach
}
