% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{cae_grid_cells}
\alias{cae_grid_cells}
\title{Map points to their grid cells}
\usage{
cae_grid_cells(points, grid)
}
\arguments{
\item{points}{data.frame with columns \code{lon} and \code{lat} in WGS84 decimal
degrees. Any other columns (such as \code{site_id}) are carried through.}

\item{grid}{a \code{stars} object from \code{\link[=cae_fetch]{cae_fetch()}} (any single variable, any
timestep). Only its grid geometry and CRS are used.}
}
\value{
\code{points} with added columns: \code{cell_i}, \code{cell_j} (1-based grid
indices), \code{cell_id} (a label like \code{"x50_y58"}), and \code{cell_lon},
\code{cell_lat} (the cell center in WGS84).
}
\description{
Snaps each lon/lat to the nearest cell of a fetched grid, so sites that
fall in the same cell collapse onto one identifier. Useful for
deduplicating downstream output: build one driver file per cell and point
every site in that cell at it. At 45 km (\code{d01}) a few thousand California
sites ride on at most a couple hundred cells.
}
\details{
Designed for the WRF (WUS-D3) grid, which uses projected \code{x}/\code{y}
coordinates. Snapping picks the same cell that \code{\link[=cae_fetch_point]{cae_fetch_point()}} would
extract.
}
\examples{
\dontrun{
grid <- cae_fetch("t2", model = "CESM2", scenario = "ssp370",
                  start_time = "2050-07-01T00:00:00",
                  end_time   = "2050-07-01T00:00:00")
sites <- data.frame(site_id = c("davis", "west_sac", "fresno"),
                    lon = c(-121.74, -121.53, -119.77),
                    lat = c(38.54, 38.58, 36.75))
cells <- cae_grid_cells(sites, grid)
length(unique(cells$cell_id))   # davis and west_sac share a 45 km cell
}
}
